import React from 'react';

const input = (props) => {
    return (
        <form>
            <input type="text" placeholder="What are you upto?" />
            <button >Add</button>
        </form>
    )
}

export default input;